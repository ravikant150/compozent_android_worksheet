void main() {
  // Step 1: Create a set of unique integers
  Set<int> numbers = {1, 2, 3, 4};

  // Step 2: Add a new number 5 to the set
  numbers.add(5);

  // Step 3: Remove the number 2 from the set
  numbers.remove(2);

  // Step 4: Print the final set
  print('Final Set: $numbers');
}